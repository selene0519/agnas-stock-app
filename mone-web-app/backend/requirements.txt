# starlette 1.0.1 미만에 인증 우회(PYSEC-2026-161)·Range DoS·폼 한도 무시가
# 몰려 있다. 2026-07-29에 0.41.3에서 관리자 인증 우회를 실제로 재현했다.
# 코드(request.scope['path'])로도 막았지만 라이브러리 자체도 올린다 —
# 코드 수정은 지금 쓰는 경로만 지키지, 나중에 StaticFiles/request.form()을
# 쓰기 시작하면 나머지 CVE가 그대로 살아난다.
# fastapi/pydantic/uvicorn은 starlette 1.x와 맞물려 같이 올라간다.
fastapi==0.140.13
uvicorn==0.51.0
starlette==1.3.1
pydantic==2.13.4
pandas==2.2.3
numpy==2.4.4
pandas_market_calendars==5.3.2
python-dotenv==1.2.2
requests==2.33.0
httpx==0.28.0
aiofiles==24.1.0
yfinance==1.3.0
finance-datareader==0.9.110
beautifulsoup4==4.14.3
lxml==6.1.0
openpyxl==3.1.5
matplotlib==3.10.9
psycopg2-binary==2.9.10
anthropic>=0.40.0
